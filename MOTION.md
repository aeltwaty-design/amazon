# Motion inventory

Every animation on the page, the token it reads, and where the code lives. Edit `styles/tokens.css` to tune timings, distances and easings; the code reads tokens at init. Progress windows and geometry constants live in the `HERO` object at the top of `components/hero/HeroChoreography.tsx`.

Conventions: **p** is the pinned-scrub timeline progress (0 = pin starts, 1 = pin releases after exactly one viewport of scroll). "Mirrors" means the horizontal component is multiplied by −1 in Arabic (`dirX`); vertical motion is never mirrored.

## Hero — load sequence (plays once on mount, before any scroll)

| ID  | Animation                       | Element                                   | Trigger                          | Duration                | Easing                             | Values                                                                                | Mirrors  | < 1024px     | Reduced motion | Code                                       |
| --- | ------------------------------- | ----------------------------------------- | -------------------------------- | ----------------------- | ---------------------------------- | ------------------------------------------------------------------------------------- | -------- | ------------ | -------------- | ------------------------------------------ |
| L1  | Logo lockup fades and scales in | `[data-hero-lockup]` (inline WalaOne SVG) | mount                            | `--hero-logo-enter`     | `--ease-out-cubic`                 | opacity 0→1, scale 0.92→1                                                             | no       | same         | static         | `HeroChoreography.tsx` `buildLoadSequence` |
| L2  | Eyebrow + headline words rise   | `[data-hero-eyebrow]`, `[data-hero-word]` | mount + `LOAD.headlineStartMs`   | `--hero-headline-enter` | `--ease-out-cubic`                 | y 24→0, opacity 0→1, stagger `LOAD.wordStaggerMs`                                     | no       | same         | static         | same                                       |
| L3  | Sub-headline + price lead rise  | `[data-hero-sub]`, `[data-hero-price]`    | mount + `LOAD.subStartMs`        | `LOAD.subMs`            | `--ease-out-cubic`                 | y 24→0, opacity 0→1                                                                   | no       | same         | static         | same                                       |
| L4  | CTAs enter with overshoot       | `[data-hero-ctas]`, `[data-hero-trust]`   | mount + `--hero-cta-enter-start` | `--hero-cta-enter`      | `--ease-out-cubic` then power2.out | y `--hero-cta-enter-distance` → `--hero-cta-settle` → 0                               | no       | same         | static         | same                                       |
| L5  | Benefit cards peek in, stacked  | `[data-hero-card]` ×5                     | with L4                          | `--hero-card-enter`     | `--ease-out-cubic`                 | y from stack + `--hero-card-enter-distance`, scale 0.94, stagger `LOAD.cardStaggerMs` | fan only | not rendered | not rendered   | same                                       |

## Hero — pinned scrub (desktop ≥ 1024 × 700, `scrub: 0.6`, pin = 100vh)

| ID  | Animation                              | Element                 | Trigger                                             | Duration | Easing | Values                                                                                                                                                       | Mirrors      | < 1024px            | Reduced motion | Code               |
| --- | -------------------------------------- | ----------------------- | --------------------------------------------------- | -------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------ | ------------------- | -------------- | ------------------ |
| S1  | Illustration collapses out of headline | `[data-hero-art]`       | p `P.art` = 0 → 0.30                                | scrub    | linear | width natural→0, opacity 1→0; headline halves close because the H1 is a flex row                                                                             | no           | time-based, see M2  | static         | `buildPinnedScrub` |
| S2  | Cards fly to the Benefits grid         | `[data-hero-card]` ×5   | p `P.cards` = 0.15 → 0.90, stagger `CARD_STAGGER_P` | scrub    | linear | x/y from stack → `Flip.fit` target − pin distance, scale 0.94→1, radius stays `--radius-card`                                                                | measured, no | not rendered        | not rendered   | same               |
| S3  | Hero text block fades up               | `[data-hero-text]`      | p `P.text` = 0.70 → 1                               | scrub    | linear | y 0→`TEXT_EXIT_Y`, opacity 1→0                                                                                                                               | no           | none                | static         | same               |
| S4  | End-state lock                         | `[data-hero-card]`      | p `P.lock` = 0.95                                   | instant  | —      | hard-sets S2's end values so a fast wheel cannot release the pin mid-flight                                                                                  | —            | —                   | —              | same               |
| S5  | Visibility swap                        | hero cards ↔ grid cards | pin release / re-entry                              | instant  | —      | mirror hidden, grid visible (and back)                                                                                                                       | —            | grid always visible | grid visible   | same, `swap()`     |
| S6  | Dark surface cross-fade (optional)     | `[data-hero]`           | `HERO_SURFACE_FADE`                                 | scrub    | linear | off by default (brief: hard cut at unpin). The surface is now the `.surface-hero` gradient; enabling this needs an opacity tween on a separate surface layer | no           | —                   | —              | same               |

## Hero — mobile and short viewports (no pin)

| ID  | Animation             | Element           | Trigger                | Duration                                                          | Easing            | Values                       | Mirrors | Reduced motion | Code          |
| --- | --------------------- | ----------------- | ---------------------- | ----------------------------------------------------------------- | ----------------- | ---------------------------- | ------- | -------------- | ------------- |
| M1  | Illustration parallax | `[data-hero-art]` | hero scrolling out     | scrub                                                             | linear            | y 0→`MOBILE_ART_PARALLAX_PX` | no      | static         | `buildMobile` |
| M2  | Illustration exit     | `[data-hero-art]` | hero bottom passes 40% | `--hero-illustration-exit` after `--hero-illustration-exit-start` | `--ease-out-expo` | width→0, opacity→0           | no      | static         | same          |

## Page

| ID  | Animation             | Element                                               | Trigger                      | Duration                                                     | Easing             | Values                                                                                                                                                | Mirrors                | Reduced motion | Code                                                     |
| --- | --------------------- | ----------------------------------------------------- | ---------------------------- | ------------------------------------------------------------ | ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------- | -------------- | -------------------------------------------------------- |
| P1  | Section reveal        | `[data-reveal]` (+ `[data-reveal-mobile]` below 1024) | enters 88% of viewport, once | `--motion-600`                                               | `--ease-out-cubic` | y 24→0, opacity 0→1, stagger 60ms                                                                                                                     | no                     | static         | `components/motion/PageMotion.tsx`                       |
| P2  | Brand row parallax    | `[data-parallax="brands-row"]`                        | section in view              | scrub                                                        | linear             | x −24→+24px                                                                                                                                           | yes                    | static         | same                                                     |
| P3  | Pricing card tilt     | `[data-tilt]`                                         | enters 80%, once             | `--motion-800`                                               | `--ease-spring`    | rotation 4°→0, origin bottom-centre                                                                                                                   | yes                    | static         | same                                                     |
| P4  | Scroll hint bounce    | `ScrollHint` arrow                                    | always                       | `--scroll-arrow-bounce`, delay `--scroll-arrow-bounce-delay` | CSS ease           | y ±`--scroll-arrow-bounce-distance`                                                                                                                   | no                     | none           | `components/ui/ScrollHint.tsx` + `hint-bounce` keyframes |
| P5  | Scroll hint flip/tone | `ScrollHint`                                          | hero pin end; dark surfaces  | `--motion-300`                                               | `--ease-out-cubic` | arrow rotates 180°; yellow/ink over the two purple bands (Hero, Final CTA), purple/white elsewhere                                                    | no                     | instant        | same                                                     |
| P6  | Header surface        | `Header`                                              | scrollY > 24                 | `--header-surface`                                           | `--ease-out-cubic` | transparent → `--color-bg-page` + hairline; both logos recolour through currentColor in the same tween (white reverse → brand colours / Amazon black) | no                     | instant        | `components/ui/Header.tsx`                               |
| P7  | Button hover          | `Button`                                              | hover                        | `--btn-hover`                                                | `--ease-out-cubic` | y −1px, radial `--color-cta-glow`                                                                                                                     | no                     | none           | `components/ui/Button.tsx`                               |
| P8  | Pill toggle thumb     | `PillToggle`                                          | selection change             | `--plans-switch`                                             | `--ease-out-cubic` | translateX × `--dir`                                                                                                                                  | CSS                    | instant        | `components/ui/PillToggle.tsx`                           |
| P9  | Benefit card hover    | `.benefit-card`                                       | hover                        | `--card-hover`                                               | `--ease-spring`    | fill shifts by `--color-card-hover-shift` toward ink, y −4px; `--color-ink-muted` stays ≥ 4.5:1 on every text-bearing pastel through the shift        | no                     | none           | `styles/globals.css`                                     |
| P10 | Price view switch     | `PlanCard` amount                                     | Before VAT / Total toggle    | `--plans-switch`                                             | `--ease-out-cubic` | `fade-up` keyframes: opacity 0→1, y 6→0                                                                                                               | no                     | none           | `components/ui/PlanCard.tsx`                             |
| P11 | FAQ panel open/close  | `ui/Faq` `[data-panel]`                               | summary click                | `--motion-300`                                               | `--ease-out-cubic` | height 0↔auto, opacity; chevron rotates 180°                                                                                                          | no                     | native toggle  | `components/ui/Faq.tsx`                                  |
| P12 | Stepper state         | `Stepper` bubbles                                     | step change                  | `--motion-300`                                               | CSS ease           | colour swap complete/current/upcoming                                                                                                                 | connector via `start-` | instant        | `components/ui/Stepper.tsx`                              |

## Boot signals

Two attributes on `<html>` gate everything above: `data-js` (the hero pre-hide only applies with JavaScript) and `data-fonts` (the page stays `visibility: hidden` until both font families are in, 1 s timeout, so the swap can never register as a layout shift). The inline script in the layout sets them before first paint; `components/motion/BootSignals.tsx` re-applies them in a layout effect after every mount, because React 19 strips all attributes from `<html>` when the locale layout remounts on a soft navigation. The hero choreography waits for `data-fonts` before it measures anything.

## Guard rails

- `prefers-reduced-motion: reduce`: no pin, no scrub, no reveals; the hero mirror is `display: none` and the in-flow grid renders as static content.
- Below 1024px wide or 700px tall: no pin; the mirror is hidden and the grid cards reveal in flow.
- Triggers are killed and rebuilt on locale change and after a debounced resize (`RESIZE_DEBOUNCE_MS`); `ScrollTrigger.refresh()` runs after `document.fonts.ready`.
- Geometry (stack and landing positions) is re-measured on every refresh with transforms cleared, so a font swap or layout change cannot leave the cards landing off-target.

## Token glossary

| Token                                                        | Changing it…                                             |
| ------------------------------------------------------------ | -------------------------------------------------------- |
| `--hero-logo-enter`                                          | how long the lockup takes to appear                      |
| `--hero-headline-enter`                                      | per-word rise duration                                   |
| `--hero-cta-enter`, `--hero-cta-enter-start`                 | CTA rise duration and when it starts                     |
| `--hero-cta-enter-distance`, `--hero-cta-settle`             | how far the CTA travels and how much it overshoots       |
| `--hero-card-enter`, `--hero-card-enter-distance`            | card peek-in duration and travel                         |
| `--hero-illustration-exit`, `--hero-illustration-exit-start` | mobile illustration exit (desktop uses the scrub window) |
| `--card-hover`                                               | benefit card hover speed                                 |
| `--plans-switch`                                             | pill toggle thumb speed                                  |
| `--scroll-arrow-bounce*`                                     | scroll hint bounce period, delay and amplitude           |
| `--motion-300/600/800`                                       | scroll hint, reveals, pricing tilt                       |
| `--btn-hover`, `--header-surface`                            | button hover and header surface transition               |
| `--ease-out-cubic`, `--ease-out-expo`, `--ease-spring`       | the three curves, shared by CSS and GSAP                 |
