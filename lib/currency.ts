// The Saudi riyal symbol as SVG path data (viewBox 0 0 1024 1024). It is null
// until the approved artwork lands; <Currency /> falls back to text meanwhile.
export const SAR_SYMBOL_PATH: string | null = null;

export const SAR_TEXT = { ar: 'ر.س', en: 'SAR' } as const;
